# Template di riferimento — la versione REALE viene generata automaticamente
# da server-setup.sh direttamente sul server (con password DB casuale) e non
# va mai copiata a mano da qui. Questo file è solo documentazione, sicuro da
# committare (nessun segreto reale dentro).

DB_HOST=localhost
DB_NAME=turni_bidelli
DB_USER=turni_bidelli_user
DB_PASS=
